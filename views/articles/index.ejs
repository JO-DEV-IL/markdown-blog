<!-- EJS file that acts as an index.html file -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    
    <!-- Bootstrap CSS Install -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">

    <!-- CSS styling using ejs include -->
    <%- include('styles') %>
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
</head>
<body>
    <!-- Bootstrap CSS: container class provides some padding around the div -->
    <div class="container">
        <!-- Bootstrap CSS: margin-bottom of 4, mt = margin-top, etc. -->
        <img src="https://i.ibb.co/8mh2d1V/orange-vector-free.png" class="img-fluid mx-auto d-block" alt="Responsive image">
        
        <h1>SLICE <span style="font-size: 100px; color: #ff7c00;">O</span>F LIFE</h1>
        <h3>Blogosphere</h3>
        
        <h2 class="mt-4">Article Slices</h2>
        <!-- Bootstrap CSS: button and button-success -->
        <!-- Link to localhost:port/articles/new -->
        <a href="/articles/new" class="btn btn-success">New Article</a>

        <!-- ESJ syntax -->
        <!-- For each interation in the Articles array, output this HTML over it -->
        <% articles.forEach(article => { %>
            <div class="card mt-4 mb-4">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>
                    <!-- Bootstrap CSS: text-muted makes font color a light grey -->
                    <div class="card-subtitle text-muted mb-2">
                        <!-- toLocaleDateString() converts 'new Date()' timestamp into local time -->
                        <%= article.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2"><%= article.description %></div>
                    <!-- added .slug instead of .id to article variable -->
                    <a href="articles/<%= article.slug %> " class="btn btn-success">Read More</a>
                    <a href="articles/edit/<%= article.id %> " class="btn btn-success">Edit</a>
                    <!-- utilizing method-override by including ?_method=DELETE despite method of form being POST which becomes irrelevant but necessary for the form requirements-->
                    <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>